# Current directory is default
# Tests run from the api folder, so default application.yml has to be overridden
qc.file-location=testfile.json

# Default update interval in milliseconds
qc.update-interval=5000